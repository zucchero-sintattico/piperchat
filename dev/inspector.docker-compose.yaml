services:
  inspector:
    image: ubuntu:latest
    tty: true
    env_file:
      - .env

    networks:
      messages-network:
      monitoring-network:
      notifications-network:
      piperchat-network:
      users-network:
      multimedia-network:
      backend:
      frontend:
